// FORMAS DE CRIAR FUNCOES NO JAVASCRIPT

// function expression

// acontece quando a função é criada e atribuída a uma variável, 
// tratando a função como se ela fosse qualquer outro valor (como um número, uma string ou um objeto).

const soma = function (a, b) {
    return a + b;
}

console.log(soma(2, 3));
